[project]
name = "textish"
version = "0.5.0"
description = "Serve Textual apps over SSH"
readme = "README.md"
license = "MIT"
authors = [
    { name = "Aisha630", email = "ayeshashafique123@gmail.com" }
]
requires-python = ">=3.12,<4.0"
dependencies = [
    "asyncssh>=2.24.0,<3.0.0",
    "textual>=8.2.0,<9.0.0",
]
keywords = ["textual", "ssh", "tui", "terminal", "asyncio", "asyncssh"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Framework :: AsyncIO",
    "Topic :: Software Development :: Libraries",
    "Topic :: Terminals",
]

[project.optional-dependencies]
color = ["colorlog>=6.9.0,<7.0.0"]
performance = ["uvloop>=0.22.0,<1.0.0"]

[project.scripts]
textish = "textish.cli:main"

[project.urls]
Homepage = "https://github.com/Aisha630/textish"
Repository = "https://github.com/Aisha630/textish"
Issues = "https://github.com/Aisha630/textish/issues"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
test = [
    "pytest>=8.0.0,<9.0.0",
    "pytest-asyncio>=1.0.0,<2.0.0",
]
lint = [
    "mypy>=1.10.0,<2.0.0",
    "ruff>=0.15.10,<0.16.0",
]
dev = [
    { include-group = "test" },
    { include-group = "lint" },
    "pre-commit>=4.0.0,<5.0.0",
]
release = [
    "python-semantic-release>=9.0.0,<10.0.0",
]

[tool.poetry.group.test]
optional = true

[tool.poetry.group.lint]
optional = true

[tool.poetry.group.dev]
optional = true

[tool.poetry.group.release]
optional = true

[tool.poetry]
packages = [
    { include = "textish", from = "src" },
]

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.semantic_release]
version_toml = ["pyproject.toml:project.version"]
build_command = "pip install poetry==2.4.1 && poetry build"

[tool.semantic_release.branches.main]
match = "main"
prerelease = false

[tool.semantic_release.publish]
dist_glob_patterns = ["dist/*"]
upload_to_vcs_release = true

[tool.mypy]
files = ["src/textish"]
python_version = "3.12"
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
